framework:
    secret:        '1234'
    charset:       UTF-8
    error_handler: null
    csrf_protection:
        enabled: false
    router:        { resource: "%kernel.root_dir%/config/routing.yml" }
    validation:    { enabled: true, annotations: false }
    session:
        lifetime:       3600
        auto_start:     true
